on:
  push:
    branches: [main]
  create:
    tags:
       - '*'
      
      
jobs:
  tests:
    runs-on: ubuntu-18.04
    steps:
      - name: get env
        run: |
          echo $GITHUB_REF_TYPE
          echo $GITHUB_REF_NAME
          echo $GITHUB_REF
